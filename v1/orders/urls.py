from django.urls import path
from .views import *

urlpatterns = [
   # path("", Order_View.as_view(), name="orders-list"),
   # path("<int:pk>/order", Order_DetailView.as_view(), name ="order-detail"),
   # path("list/", OrderList_APIView.as_view(), name="order-items")
   
]
